# vagrant-centos-ubuntu-webstack
Multi-VM infrastructure setup using Vagrant to provision a CentOS static website and an Ubuntu WordPress application with automated configuration and firewall management.
